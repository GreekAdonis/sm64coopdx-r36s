Source code for CoopNet is available at https://github.com/coop-deluxe/coopnet.

The ARM64 archive is built from commit `9d9b3dd` with
`executable-hash-override.patch`. This lets source-built Linux ARM64 ports use
the official release compatibility token required by the public lobby server.
